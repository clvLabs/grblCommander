# This file is used to store common commands thru the macro files
# It will not be treated as a macro, as it doesnt' export a 'macro' object
# To use it into your macro files, add 'from .vars import *'

SAFE_HEIGHT = 'G0 Z8'
RAPID_00    = 'G0 X0 Y0'
